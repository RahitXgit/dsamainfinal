/** @type {import('next').NextConfig} */
const nextConfig = {
    /* config options here */
    // reactCompiler: true, // Commented out as likely not supported in Next 14 or experimental
};

export default nextConfig;
